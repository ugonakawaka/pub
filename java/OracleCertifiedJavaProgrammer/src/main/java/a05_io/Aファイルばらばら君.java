package a05_io;

import java.nio.file.Path;

public class Aファイルばらばら君 {

	public static void main(String[] args) {
		a(null);
	}

	static void a(Path path) {
		// 指定されたファイルを、指定サイズにばらばらにします。
		// ばらばらにするときに、は指定されたファイルのあるディレクトリに日付(分まで)ディレクトリを作成
		// そこに作成する
		var MB = 1;
		final var SIZE = (long) (1024 * 1024) * MB;

		
		
		
		
	}
}
